# AI 使用说明

## 使用了什么

本作业使用 Minis 辅助开发，并显式采用三个技能：

- `grill-me`：分三轮确认范围、数据口径、AI 权限、测试缝和交付标准；
- `tdd`：在已确认的导入命令、HTTP API、页面和健康检查公共缝上逐切片执行红—绿；
- `production-agent-public`：检查错误处理、持久化、日志、健康检查、模型降级和 Docker 交付。

DeepSeek V4 Flash 是产品运行时的自然语言规划器，不是代码生成的唯一来源。

后续维护使用 Codex，并通过 `grill-me` 逐项确认交互、安全、证据展示和演示口径，再用 `tdd` 从公开 HTTP/页面行为复现并修复问题。真实浏览器验收覆盖桌面和手机视口。

## 真实 Prompt 示例

> 使用 Django 和 SQLite 完成门店销售看板。模型不能读 CSV 后直接回答，也不能执行任意 SQL；它只能选择白名单查询工具，所有数字必须由 ORM 聚合并附可核查证据。先为“牛肉poke六月卖了多少钱？”写失败的 HTTP 契约测试，再写最小实现。

## AI 风险、错误与修复

开发中真实 DeepSeek 冒烟返回：

```json
{"tool":"query_product_revenue","args":{"product":"牛肉poke","month":6}}
```

这暴露了一个真实契约风险：本地执行器还需要 `year`，模型可能省略它。执行层因此不盲信参数：出现 `KeyError`、类型错误、非法工具或非法 JSON 时，丢弃计划并回退到确定性解析器。测试还模拟了 `run_sql` 非法工具和连续三次网络失败，确认系统不会执行模型自由文本或编造数字。

另一个开发期错误是 Django 脚手架的 `analytics/tests.py` 与测试包 `analytics/tests/` 重名，测试发现导入冲突后删除旧文件并重新验证；没有隐藏失败过程。

维护期再次发现：页面 JavaScript 会从 `csrftoken` Cookie 构造请求头，但模板从未渲染 CSRF Token，真实浏览器首次打开页面时不会获得 Cookie，导致问答 POST 被 Django 拒绝。通过启用 CSRF 校验的页面测试复现后，在表单公共界面补齐 Token；同时为加载、失败重试和完整证据显示补上浏览器验收。

手机验收又发现两个容易被普通接口测试漏掉的问题：CSRF 隐藏字段成为表单里的第一个 `input` 后，宽泛选择器会禁用令牌而不是问题输入框；改为唯一输入框 ID 后，390px 视口确认输入框和发送按钮会一起进入忙碌状态。另一个问题是 `DEBUG=true` 的技术 404 会列出随机 Admin 路径；现在 `/admin` 探测路径使用固定的简洁 404，且测试保证响应中不出现真实入口。

## 防幻觉设计

1. 模型只规划，不持有数据库连接；
2. 工具名白名单，拒绝任意 SQL；
3. Django 校验参数，ORM JOIN 维表；
4. 回答使用查询结果的确定性模板，经营数字不由模型自由生成；
5. 保存证据、筛选条件、模式和追踪 ID；
6. 不存在的数据返回“无匹配记录”，不伪装成 0；
7. 外部服务失败时明确降级。

## 人工拍板

开发者最终决定了：订单数按去重订单号、脏记录隔离规则、“最近”按最新两个完整月份、默认最新 30 天、亮点采用经营雷达，以及不为作业增加登录系统。AI 负责加速实现和查漏，但业务口径由人工确认并通过独立聚合核验。

## 自己保留控制的部分

数据清洗口径、ORM 聚合结果复核、密钥管理和 Git 提交均由开发流程直接控制。`.env` 从本地环境注入且被忽略，未把任何密钥交给文档或提交历史。
